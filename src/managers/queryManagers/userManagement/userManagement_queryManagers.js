const { constants: { userManagementConstants: { userConstants: { userRoles, departments, designations } } } } = require("../../../utils")

const fetchDepartments_queryManager = async (req) => { 
    

}