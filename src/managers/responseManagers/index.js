const errorResponseManager = require("./error_responseManager");
const successResponseManager = require("./success_responseManager");

module.exports = {
    errorResponseManager,
    successResponseManager
}