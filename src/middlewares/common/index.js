const emailMiddlewares = require('./email_middlewares');
const contactNumberMiddlewares = require('./contactNumber_middlewares');
const fieldDataMiddlewares = require('./fieldData_middlewares');

module.exports = {
    emailMiddlewares,
    contactNumberMiddlewares,
    fieldDataMiddlewares
};
